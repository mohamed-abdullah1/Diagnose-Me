## TODO
```
dotnet user-secrets init
dotnet user-secrets set "JwtSettings:Key" "<16-byte secret here>"
dotnet user-secrets set "MailSettings:Mail" "<SMTP Mail"
dotnet user-secrets set "MailSettings:Password" "<SMTP_PAssword>"
```